# miscelanous files to be deployed on all hosts below an example.
# note that the users could also specify the source as in salt://path instead of the contents
#files:
#  gpl_host_pinning:
#    name: /etc/apt/preferences.d/gplhost
#    contents: |
#      "Package: *\nPin: origin archive.gplhost.com\nPin-Priority: 999"
